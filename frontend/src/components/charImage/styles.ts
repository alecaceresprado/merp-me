import { makeStyles } from "@material-ui/core";

export default makeStyles({
  imageContainer: {
    backgroundColor: 'dimgrey',
    borderRadius: '10rem',
    width: '10rem',
    height: '10rem',
    border: 'groove',
    zIndex: 1
  }
});